import { NewLabel } from "@components/new_label";

export default {
  google: "Google",
  microsoft: "Microsoft",
  notion: "Notion",
  odoo: <div key="odoo">"Odoo" {NewLabel}</div>,
  RSS: <div key="rss">"RSS" {NewLabel}</div>,
  salesforce: <div key="salesforce">"Salesforce" {NewLabel}</div>,
};
